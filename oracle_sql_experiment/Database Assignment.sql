DROP TABLE PRODUCTS;
CREATE TABLE PRODUCTS (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME VARCHAR2(50),
    CATEGORY VARCHAR2(50),
    PRICE NUMBER(8,2)
);

INSERT INTO PRODUCTS (NAME, CATEGORY, PRICE) VALUES ('Laptop', 'Electronics', 3500);
INSERT INTO PRODUCTS (NAME, CATEGORY, PRICE) VALUES ('Mouse', 'Electronics', 50);
INSERT INTO PRODUCTS (NAME, CATEGORY, PRICE) VALUES ('Keyboard', 'Electronics', 120);
INSERT INTO PRODUCTS (NAME, CATEGORY, PRICE) VALUES ('Chair', 'Furniture', 300);

COMMIT;

DROP TABLE USERS;
CREATE TABLE USERS (
    USER_ID   NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    USERNAME  VARCHAR2(50) NOT NULL,
    PASSWORD  VARCHAR2(50) NOT NULL,
    ROLE      VARCHAR2(20)
);

INSERT INTO USERS (USERNAME, PASSWORD, ROLE)
VALUES ('admin', 'admin123', 'ADMIN');

INSERT INTO USERS (USERNAME, PASSWORD, ROLE)
VALUES ('user1', 'password1', 'USER');

COMMIT;
